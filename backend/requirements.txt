# --- core (lightweight; runs on any laptop, no GPU) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
psycopg2-binary>=2.9        # Postgres driver — set DATABASE_URL to a Supabase/Postgres URL to PERSIST data across restarts
python-multipart>=0.0.9      # file upload
openpyxl>=3.1               # Excel export

# --- PDF render + image crops (CPU only) ---
PyMuPDF>=1.24
Pillow>=10.0               # zoom re-read crops/upscales low-confidence regions

# --- cloud model providers (only needed when EXTRACTOR/OCR != stub) ---
openai>=1.30               # OpenAI vision read (or OpenAI-compatible / vLLM via base_url)
mistralai>=1.0             # Mistral OCR 4 (boxes + per-word confidence)

# --- dev / test ---
pytest>=8.0
httpx>=0.27
