name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend deps
        working-directory: frontend
        run: npm ci --ignore-scripts

      - name: Build for GitHub Pages
        working-directory: frontend
        env:
          # Backend URL baked into the static build so visitors auto-connect with
          # no setup. Set it in repo Settings → Secrets and variables → Actions →
          # Variables → VITE_API_BASE (e.g. the Render URL, or the tunnel interim).
          # Empty = users still configure at runtime via the gear or go.html?api=…
          VITE_API_BASE: ${{ vars.VITE_API_BASE }}
        run: npx vite build --config vite.config.pages.ts

      - name: Add SPA 404 fallback
        run: cp frontend/dist/index.html frontend/dist/404.html

      - uses: actions/configure-pages@v4

      - uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
